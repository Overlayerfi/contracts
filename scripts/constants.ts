export const HARDHAT_CHAIN_ID = 31337;

export const ETH_MAINNET_TOKEN_DECIMALS: { [tokenName: string]: number } = {
  USDC: 6,
  USDT: 6,
  EURS: 2
};

export const SEPOLIA_TOKEN_DECIMALS: { [tokenName: string]: number } = {
  USDC: 6,
  USDT: 6,
  EURS: 2
};
